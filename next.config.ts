import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images : {
    domains: [
      "img.daisyui.com",
      "images.unsplash.com",
      "source.unsplash.com",
      "images.pexels.com",
      "cdn.pixabay.com",
      "cdn.pixabay.com",
      "images.unsplash.com",
      "images.unsplash.com",
    ],
  },
};

export default nextConfig;
